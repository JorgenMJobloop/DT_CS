```cs
        for (int i = 0; i < myArray.Length; i++)
        {
            Console.WriteLine(myArray[i]);
        }

        for (int j = 0; j < myIntArray.Count(); j++)
        {
            Console.WriteLine(myIntArray[j]);
        }

```